name: Rust

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Build
      run: cargo build --release
    - name: Touch binary to mark as modified
      run: touch target/release/fsorg
    - name: Create GitHub Release
      uses: pipe-cd/actions-gh-release@v2.6.0
      with:
        release_file: target/release/fsorg
        token: ${{ secrets.GITHUB_TOKEN }}
              